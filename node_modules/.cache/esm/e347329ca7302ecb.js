let express,dotenv,colors,connectDB;_a73‍.w("express",[["default",["express"],function(v){express=v}]]);_a73‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_a73‍.w("colors",[["default",["colors"],function(v){colors=v}]]);_a73‍.w("./config/db",[["default",["connectDB"],function(v){connectDB=v}]]);




//Dot ENV config
dotenv.config();

// mongodb connection
connectDB();

//rest object
const app = express();

//middelwares
app.use(express.json());

//routes
app.get("/", (req, res) => {
  res.send("Server is running...");
});

//port
const PORT = process.env.PORT || 5000;

//listen
app.listen(PORT, () => {
  _a73‍.g.console.log(
    `Node Server Running In ${process.env.NODE_ENV} Mode on port no ${PORT}`
      .bgCyan.white
  );
});
